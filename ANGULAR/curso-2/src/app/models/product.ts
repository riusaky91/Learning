export default interface Product {// se crea la interfaz Product   
    id: number;
    name: string;
    price: number;
    isForSale: boolean;
    description: string;
    category: string;
    image: string;
}